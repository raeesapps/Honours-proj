function styles(theme) {
  return {
    container: {
      display: 'flex',
      flexWrap: 'wrap',
    },
    fab: {
      margin: theme.spacing(5),
    },
    button: {
      margin: theme.spacing(2),
    },
  };
}

export default styles;
