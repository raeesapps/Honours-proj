function styles(theme) {
  return {
    topLeft: {
      position: 'absolute',
      top: 0,
      left: 0,
      textAlign: 'left',
      marginLeft: theme.spacing(1),
    },
  };
}

export default styles;
