function styles(theme) {
  return {
    root: {
      marginTop: theme.spacing(0),
    },
    instructions: {
      marginBottom: theme.spacing(2),
    },
    paper: {
      marginBottom: theme.spacing(2),
    },
  };
}

export default styles;
